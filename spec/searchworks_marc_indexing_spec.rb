require "spec_helper"

describe SearchworksMarcIndexing do
  it "has a version number" do
    expect(SearchworksMarcIndexing::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
